############################# Makefile ##########################
CC = g++
CTARGET = src/main.cpp
EXECNAME = vigenere.exe

all: 
	$(CC) -o $(EXECNAME) $(CTARGET)
	@echo "Execute using: ./vigenere.exe {-c, -d} tests/<FILE>.txt"
	
clean: $(RM) $(vigenere.exe)